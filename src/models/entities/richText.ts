// src/models/entities/richText.ts
import type { JSONContent } from '@tiptap/core';

/**
 * Rich text content stored as TipTap JSONContent.
 * Used in entity documents for freeform prose sections.
 */
export type RichText = JSONContent;
